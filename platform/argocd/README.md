# Argo CD Bootstrap

This folder contains a bootstrap Application that points to the GitOps overlays.

1. Install Argo CD using the official manifests.
2. Apply `bootstrap.yaml` to let Argo CD manage the rest of the platform.
